# Local Supabase — values come from the output of `npx supabase start`
# (or `npx supabase status` at any time). Copy this file to `.env`.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=paste-the-anon-key-printed-by-supabase-start
